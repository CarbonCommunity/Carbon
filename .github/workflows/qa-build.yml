name: QA Build
concurrency: build-qa

on:
  workflow_dispatch:

jobs:
  build:
    name: QA Build
    runs-on: ubuntu-latest
    container: rust:bullseye
    continue-on-error: false

    outputs:
      date: ${{ steps.step1.outputs.date }}
      clock: ${{ steps.step1.outputs.clock }}
      tag: ${{ steps.step1.outputs.tag }}
      ref: ${{ steps.step1.outputs.ref }}
      version: ${{ steps.step1.outputs.version }}

    steps:
      - name: Environment Setup
        id: step1
        shell: bash
        run: |
          echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT
          echo "clock=$(date +'%H:%M:%S')" >> $GITHUB_OUTPUT
          echo "tag=$(date +'%Yd%j')" >> $GITHUB_OUTPUT
          echo "ref=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "version=2.9.$(curl -s -L 'https://carbonmod.gg/version?id=qa&token=${{secrets.VERSIONS_TOKEN}}')" >> $GITHUB_OUTPUT
          git config --global --add safe.directory /__w/Carbon/Carbon

      - name: Git Clone
        uses: actions/checkout@v3
        with:
          ref: qa
          token: ${{ secrets.PAT_ALL_REPO }}
          submodules: recursive

      - name: Rust Setup
        shell: bash
        run: |
          apt update
          apt install -y mingw-w64

          rustup install stable
          rustup default stable
          rustup target add x86_64-pc-windows-gnu
          rustup component add rust-std --target x86_64-pc-windows-gnu
          rustup show
          rustc --print sysroot

          which x86_64-w64-mingw32-dlltool || echo "dlltool not found"
          x86_64-w64-mingw32-dlltool --version

      - name: Dotnet Setup
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: 8.0.406

      - name: Carbon.Native Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Carbon.Native Build (Linux)
        uses: ClementTsang/cargo-action@v0.0.3
        with:
          command: build
          args: -r --target x86_64-unknown-linux-gnu
          directory: Carbon.Core/Carbon.Native

      - name: Carbon.Native Build (Windows)
        uses: ClementTsang/cargo-action@v0.0.3
        with:
          command: build
          args: -r --target x86_64-pc-windows-gnu
          directory: Carbon.Core/Carbon.Native

      - name: Carbon.Native Build (Linux) (PROD)
        uses: ClementTsang/cargo-action@v0.0.3
        with:
          command: build
          args: --profile prod --target x86_64-unknown-linux-gnu
          directory: Carbon.Core/Carbon.Native

      - name: Carbon.Native Build (Windows) (PROD)
        uses: ClementTsang/cargo-action@v0.0.3
        with:
          command: build
          args: --profile prod --target x86_64-pc-windows-gnu
          directory: Carbon.Core/Carbon.Native

      - name: Carbon Bootstrap
        shell: bash
        run: |
          dotnet run --project Carbon.Core/Carbon.Tools/Carbon.Runner Tools/Build/runners/bootstrap.cs
          dotnet run --project Carbon.Core/Carbon.Tools/Carbon.Runner Tools/Build/runners/update.cs

      - name: Carbon Build
        shell: bash
        run: |
          export VERSION=${{ steps.step1.outputs.version }}
          dotnet run --project Carbon.Core/Carbon.Tools/Carbon.Runner Tools/Build/runners/build.cs Release QA qa_build
          dotnet run --project Carbon.Core/Carbon.Tools/Carbon.Runner Tools/Build/runners/build.cs Debug QA qa_build
          dotnet run --project Carbon.Core/Carbon.Tools/Carbon.Runner Tools/Build/runners/build.cs Minimal QA qa_build
          dotnet run --project Carbon.Core/Carbon.Tools/Carbon.Runner Tools/Build/runners/build.cs ReleaseUnix QA qa_build
          dotnet run --project Carbon.Core/Carbon.Tools/Carbon.Runner Tools/Build/runners/build.cs DebugUnix QA qa_build
          dotnet run --project Carbon.Core/Carbon.Tools/Carbon.Runner Tools/Build/runners/build.cs MinimalUnix QA qa_build

      - name: Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: |
            Release/Carbon.Linux.Debug.tar.gz
            Release/Carbon.Linux.Debug.info
            Release/Carbon.Linux.Release.tar.gz
            Release/Carbon.Linux.Release.info
            Release/Carbon.Linux.Minimal.tar.gz
            Release/Carbon.Linux.Minimal.info
            Release/Carbon.Windows.Debug.zip
            Release/Carbon.Windows.Debug.info
            Release/Carbon.Windows.Release.zip
            Release/Carbon.Windows.Release.info
            Release/Carbon.Windows.Minimal.zip
            Release/Carbon.Windows.Minimal.info

  release-on-commit:
    name: QA Release
    needs: ["build"]
    runs-on: ubuntu-latest

    steps:
      - name: 🗑️ Delete existing release tag
        uses: dev-drprasad/delete-tag-and-release@92d94d77429cb0436597047a92369526a5824f50
        with:
          delete_release: true
          tag_name: qa
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 💤 Sleep for 5 seconds
        run: sleep 5s
        shell: bash

      - name: ⬇️ Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: Release

      - name: Display structure of downloaded files
        run: ls -R
        working-directory: Release

      - name: 🏷️ Update release tag
        uses: softprops/action-gh-release@a92196038060e0c673cf3ba5b98710dd716731df
        with:
          draft: false
          prerelease: true
          tag_name: qa
          name: "QA Build — v${{ needs.build.outputs.version }}"
          body: |
            This is a manually triggered build of Carbon based on the `qa` branch.
            This build is high priority and dedicated to reflect what's to come in the upcoming `production` build. Primarily used by Quality Assurance community members.
                    
            This build is targeted at developers or testers.
            The general public is advised to use one of the [stable] builds.
            Since it's manually triggered, we're confident you should use this build to test out the latest Carbon changes.

            ### How to install
            1. Download the `Carbon.[Windows|Linux].Debug` archive from the attachments below.
            2. Unzip the archive to the root of your Rust Dedicated Server.
            3. Restart the server and enjoy.

            [stable]: https://github.com/CarbonCommunity/Carbon.Core/releases/latest
          files: |
            Release/build/Carbon.Linux.Debug.info
            Release/build/Carbon.Linux.Debug.tar.gz
            Release/build/Carbon.Linux.Release.info
            Release/build/Carbon.Linux.Release.tar.gz
            Release/build/Carbon.Linux.Minimal.info
            Release/build/Carbon.Linux.Minimal.tar.gz
            Release/build/Carbon.Windows.Debug.info
            Release/build/Carbon.Windows.Debug.zip     
            Release/build/Carbon.Windows.Release.info
            Release/build/Carbon.Windows.Release.zip     
            Release/build/Carbon.Windows.Minimal.info
            Release/build/Carbon.Windows.Minimal.zip
